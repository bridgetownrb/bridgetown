<wa-callout variant="{{ type }}" appearance="outlined filled">
  <wa-icon
    slot="icon"
    library="remixicon"
    name="{{ icon }}"
    style="font-size:1.25em"
  ></wa-icon>
  {{ content | markdownify }}
</wa-callout>
